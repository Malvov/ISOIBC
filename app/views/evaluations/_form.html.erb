
<%= simple_form_for(@evaluation) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :employee, label: 'Operario/a', prompt: 'Escoger operario/a' %>
    <%# label_tag 'Sectores' %>
    <%# select_tag 'zone_id', options_from_collection_for_select(Zone.all, 'id', 'name'), class: 'form-control form-group'  %>
    <%= label_tag 'Actividades' %>
    <%= f.grouped_collection_select :task_id, Zone.all, :tasks, :name, :id, :name, { include_blank: 'Escoger actividad' }, 
    { class: 'form-control form-group', required: true } %>
    <%= f.input :result, label: false, collection: ['Bueno', 'Regular', 'Deficiente'], prompt: 'Calificación' %>
    <%= f.input :comment, label: 'Observaciones' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Calificar', class: 'btn btn-success' %>
  </div>
<% end %>
