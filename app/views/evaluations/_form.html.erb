
<%= simple_form_for(@evaluation) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :employee %>
    <%= f.input :task_id, label: false, collection: Task.all, label_method: :name, value_method: :id %>
    <%= f.input :result, label: false, collection: %w[Bueno Deficiente], prompt: "Escoger calificación" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
