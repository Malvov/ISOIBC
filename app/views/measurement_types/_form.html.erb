
<%= simple_form_for(@measurement_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: 'Nombre' %>
    <%= f.association :equipment, prompt: 'Equipo' %>
    <%= f.association :parameter, prompt: 'Parámetro' %>
    <%= f.input :unit, label: 'Unidad de medida' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
